/* 
import { describe, it, expect } from "vitest";

describe("getLangIso3Letters", () => {
  it("returns 'eng' when the language is English", () => {
    expect(get("en")).toBe("eng");
  });

  it("returns 'spa' when the language is Spanish", () => {
    expect(getLangIso3Letters("es")).toBe("spa");
  });

  it("defaults to 'spa' for any other language", () => {
    expect(getLangIso3Letters("fr")).toBe("spa");
    expect(getLangIso3Letters("")).toBe("spa");
  });
});
*/